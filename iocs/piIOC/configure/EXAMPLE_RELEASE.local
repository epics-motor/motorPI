# Use the following lines if motorPi was built inside motor
MOTOR=
-include $(MOTOR)/configure/RELEASE
# Use the following lines if motorPi was built outside motor
#!MOTOR_PI=
#!-include $(MOTOR_PI)/configure/RELEASE.local
